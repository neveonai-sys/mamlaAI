# 05 — Changelog and Incremental Improvement Plans

This document summarizes the **code review outcomes**, **changes already made**, and **incremental plans** so that any developer or AI can continue work without breaking flow.

---

## 1. Code Review Summary (What Was Found)

### Backend (Legalv1)

- **Dual auth:** Legacy JWT/OTP and Supabase coexisted; many endpoints were still wired to JWT or unused OTP flows.
- **Security:** Hardcoded OTP bypass (`'1234'`), test endpoints without auth, password reset without proper verification, FRONTEND_URL and send-email URLs hardcoded.
- **Redundancy:** Duplicate `update_last_activity` in session_manager; commented-out `models.py` across apps; duplicate `check_auth`/onboard logic between `views.py` and `supabase_views.py`.
- **Unused code:** JWT decorator and all JWT/OTP views and URLs (otp-login, send-phone-otp, refresh-token, logout, list-session, etc.) were no longer used by the frontend.
- **Missing:** Health endpoint for monitoring; env-based configuration for frontend URL and Celery callbacks.
- **Quality:** Large functions (e.g. WhatsApp webhook), magic numbers, inconsistent error response shape. WhatsApp left as-is (low priority).

### Frontend (frontend_webpack)

- **Hardcoded URLs:** `SendEmailComponent` used `http://127.0.0.1:8000`; AxiosInstance used fixed production URL.
- **Bugs:** Navbar `handleLogoutConfirm` called undefined `onLogout` (should be `doLogout`); Layout had `marginLeft: open` (boolean instead of pixel value; later reverted to no margin to fix layout).
- **UX:** `alert()` used for success/error in SendEmailComponent; no global error boundary.
- **Unused:** `/unused/` folder contained old Login, Signup, ForgetPassword, TalkDoc variants; not in any route (no bundle impact).

---

## 2. What Was Done (Completed Changes)

### Backend

| Change | Location | Purpose |
|--------|----------|---------|
| Removed all JWT/OTP endpoints and views | `users/urls.py`, `users/views.py` | Single auth system (Supabase only) |
| Deleted `auth_decorators.py` | Legalv1 root | No remaining JWT usage |
| Removed duplicate `update_last_activity` | `users/routes/session_manager.py` | Single implementation |
| Added `GET /api/health/` | `core/views.py`, `Legalv1/urls.py` | Monitoring/load balancer |
| FRONTEND_URL from env | `Legalv1/settings.py` | `os.getenv('FRONTEND_URL', 'https://mamla.ai')` |
| Redirect and callbacks use FRONTEND_URL | `users/views.py`, `users/routes/usermetadata.py`, `users/tasks.py`, `calendar_management/tasks.py`, `create_drafts/tasks.py` | No hardcoded mamla.ai |
| Added `core/response_utils.py` | `error_response(message, status, detail)` | Optional standard error helper (not applied to existing views) |
| Removed dead import | `create_drafts/views.py` | Dropped `auth_decorators.jwt_required` |
| Cleaned commented auth_decorators refs | `calendar_management`, `todaysupdates`, `search_facility` views | Cleaner codebase |
| **Centralised LLM calls** | `core/llm_client.py` (new) · `ai_draft/tasks.py` · `talkdoc/views.py` · `utilities/routes/utils.py` · `Legalv1/settings.py` | All LLM calls go through `core.llm_client.chat_complete()`. Supports OpenAI + OpenRouter. Per-app model routing via `APP_OPENROUTER_MODELS` / `APP_OPENAI_MODELS` dicts. Provider switchable via `LLM_DEFAULT_PROVIDER` env. Fixed `ai_draft` draft-generation temperature (0.7 → 0.3) and added strict JSON-only prompt guard. `utilities` now uses SDK instead of raw `requests.post`. |

### Frontend

| Change | Location | Purpose |
|--------|----------|---------|
| API base URL from env | `AxiosInstance.jsx`, `webpack.prod.js` | `REACT_APP_API_BASE_URL` for production |
| SendEmail uses AxiosInstance + Snackbar | `SendEmailComponent.js` | No hardcoded URL; proper feedback and loading |
| Navbar logout fix | `Navbar.jsx` | `handleLogoutConfirm` calls `doLogout()` |
| Layout main content | `Layout.jsx` | No extra `marginLeft` (reverted) so content is not pushed off-screen |

### eCourts scraper and docs

| Change | Location | Purpose |
|--------|----------|---------|
| New Django app `ecourts_scraper` | `Legalv1/ecourts_scraper/` | Live eCourts data: case by CNR, advocate search, cache, async jobs |
| Agentic scrape flow | `agent/state_machine.py`, `scrapers/highcourt.py`, `districtcourt.py` | State machine + HC/DC scrapers; CAPTCHA, rate limit, cache |
| API under `/api/ecourts/` | `ecourts_scraper/views.py`, `urls.py` | GET case/<cnr>, POST refresh, GET jobs/<job_id>, POST search |
| Celery queues + beat | `Legalv1/celery.py`, `settings.py` | ecourts_realtime, ecourts_background; cleanup + health-check beat tasks |
| Docs and Cursor rule | `docs/06-ecourts-scraper.md`, `docs/README.md`, `04-api-reference.md`, `02-backend-legalv1.md`, `.cursor/rules/docs-first.mdc` | Single source of truth for eCourts; AI reads docs first before editing code |
| Global Error Boundary | `ErrorBoundary.jsx`, `index.js` | Catch React errors, show fallback UI |
| Removed unused LogoutButton | Deleted `LogoutButton.js`, export from layout index | Was calling removed JWT logout |
| Fixed Supabase password-reset link not showing reset form | `frontend_webpack/src/AppContent.js`, `Legalv1/users/routes/usermetadata.py` | Supabase recovery email lands on root URL (`/`) with `#type=recovery` hash. Added one-time `useEffect` in `AppContent.js` that detects `type=recovery` in hash on any path and navigates to `/reset-password` preserving the hash. Backend `generate_password_reset_link` updated to use `options=` keyword arg and logs the redirect URL. Note: also add `https://mamla.ai/reset-password` and `https://www.mamla.ai/reset-password` to Supabase dashboard → Authentication → URL Configuration → Allowed Redirect URLs so the email link goes directly to `/reset-password`. |
| Fixed `<button>` nested inside `<button>` in CaseCard | `frontend_webpack/src/components/ecourts/common/CaseCard.jsx` | `IconButton` (renders `<button>`) was inside `CardActionArea` (also renders `<button>`). Replaced `IconButton` with a purely decorative `Box` (`pointerEvents: none`). Eliminates React DOM nesting warning. |
| Added back button and breadcrumb navigation to CaseDetail | `frontend_webpack/src/components/ecourts/CaseDetail.jsx` | `ArrowBackIcon` `IconButton` using `navigate(-1)`. Breadcrumb "Cases" link also uses `navigate(-1)` so returning from case detail restores the correct search page. |
| Added sessionStorage search cache (30-min TTL) | `frontend_webpack/src/components/ecourts/common/useSearchCache.js` (new), `CaseSearch.jsx`, `LawyerSearch.jsx`, `LitigantSearch.jsx` | `saveSearchCache` / `loadSearchCache` / `loadLastSearchCache` helpers. On back-navigation, cached results are restored instantly without an API call. On blank mount (no URL query), last-used search is restored. Clears on tab close (sessionStorage). |
| Fixed order PDF download: `window.open()` → Axios blob | `frontend_webpack/src/components/ecourts/CaseDetail.jsx`, `ecourtsApi.js` | `window.open()` is a plain browser navigation that does NOT send the `Authorization: Bearer` header → 401. Fixed: `downloadOrder` now uses `responseType: 'blob'`; `handleDownload` fetches blob via Axios, creates object URL, triggers `<a download>` click, then revokes URL. Added `downloadingIdx` spinner and `downloadError` alert in Orders tab. |
| Fixed order PDF download: backend returns binary not JSON | `Legalv1/ecourts_api/client.py`, `Legalv1/ecourts_api/views.py` | `client.get_order()` called `_handle_response()` → `resp.json()` on binary PDF → `JSONDecodeError` → backend 500. Added `get_order_stream(cnr, filename)` that returns raw `(bytes, content_type)` without JSON parsing. `download_order` view rewritten to proxy PDF as `HttpResponse` with `Content-Disposition: attachment`. |
| Created `docs/00-agent-quickref.md` | `docs/00-agent-quickref.md` | Single-page cheat sheet for AI agents: all file paths, collections, API prefixes, routes, env vars, conventions. Agents read this instead of scanning the codebase. |
| Created `.github/copilot-instructions.md` | `.github/copilot-instructions.md` | Auto-loaded by VS Code GitHub Copilot on every task. Enforces docs-first workflow, doc maintenance protocol, and hard constraints (no JWT, no ORM, no hardcoded URLs). |
| Strengthened `.cursor/rules/docs-first.mdc` | `.cursor/rules/docs-first.mdc` | Added 4-step workflow (quickref → detail doc → change → update docs), full doc-maintenance table, and hard constraints. |
| Updated `docs/README.md` | `docs/README.md` | Added `00-agent-quickref.md` as the first and most important entry in the doc map. |
| eCourts search landing pages show pre-populated defaults | `Legalv1/ecourts_api/tasks.py` (new), `ecourts_api/views.py`, `ecourts_api/urls.py`, `Legalv1/settings.py`, `ecourtsApi.js`, `CaseSearch.jsx`, `LawyerSearch.jsx`, `LitigantSearch.jsx` | Users see real case data immediately when landing on eCourts search pages without typing anything. Three Celery Beat tasks store page-1 results from broad seed queries into `EcourtsCacheManager` under `defaults:cases`, `defaults:litigants`, `defaults:lawyers`. New `GET /api/ecourts/defaults/<section>/` endpoint reads them. Frontend falls back to this API when no sessionStorage cache and no URL query. Defaults show an info chip banner; pagination is suppressed in default view. Cases/litigants refresh daily; lawyers weekly (stable data). Pages 3+ and explicit searches always hit the live partner API. |
| Fixed download error message always showing generic fallback | `frontend_webpack/src/components/ecourts/CaseDetail.jsx` | When `responseType:'blob'` is set in Axios, any error response body is delivered as a `Blob` (not a parsed object). `err.response?.data?.error` was always `undefined`, so the catch block always showed "Download failed. Please try again." regardless of the actual error. Fixed `handleDownload` catch block to detect `instanceof Blob`, read it via `.text()`, parse as JSON, and extract `json.error` or `json.message`. Now the real partner-API error message (e.g. "Unable to process document request") is shown to the user. |

| **Complete UI Rebuild — mamlaAI_ground_zero** | `mamlaAI_ground_zero/frontend/` | Pixel-close Tailwind CSS v3 rebuild (no MUI) matching 11 Stitch design screens. Greenfield project: React 18, Webpack 5, Redux Toolkit, Supabase auth, FullCalendar, react-dropzone, react-pdf, DOMPurify, @tailwindcss/forms. Design tokens: `primary #b45e08`, `ivory #fcfaf8`, `ink #1c140d`. All 11 screens implemented: LandingPage, Login/Signup/ResetPassword, Dashboard, CommandCenter, DraftingWorkspace (3-pane), DocumentWorkspace (TalkDoc), CalendarPage (FullCalendar), CourtUpdates, Sessions, ClientOnboarding, Feedback, and 6 eCourts pages (Home/CaseSearch/CaseDetail/LawyerSearch/LitigantSearch/CauseListBrowser). Build output: `npm run build` compiles successfully with 0 errors. Dev server on port 3001, `/api` proxied to `:8000`. |
| **UI-Backend Integration Pass (mamlaAI_ground_zero)** | Multiple files — see rows below | Restored all legacy data flows in the new UI, fixed crashes, and aligned API calls with actual backend response shapes. |
| `core/views.py` `dashboard_home` — add `upcoming_events_list` | `Legalv1/core/views.py` | Dashboard agenda now receives real event objects `[{id, title, start, event_type}]` in addition to the count field. |
| `calendar_management/views.py` `events_rest` POST — forward all legacy fields | `Legalv1/calendar_management/views.py` | Event creation now passes `partyBEmail`, `meetingtype`, `meetinglink`, `caseId`, `Task_type`, `courtName`, `courtNumber`, `clientName`, `judgeName`, `allDay`, `send_remainder`, `occurrence` to the legacy layer. `event_detail_rest` PUT also normalises `taskType → Task_type`. |
| `Dashboard.jsx` — real agenda data | `mamlaAI_ground_zero/frontend/src/components/dashboard/Dashboard.jsx` | `agendaItems` sourced from `data.upcoming_events_list`; no hardcoded fallback items; empty state shown when no events. |
| `CommandCenter.jsx` — remove hardcoded events | `mamlaAI_ground_zero/frontend/src/components/dashboard/CommandCenter.jsx` | Upcoming events section uses live API data. Draft navigation uses `draft.session_id` instead of `draft.id`. |
| `DraftingWorkspace.jsx` — full intake form | `mamlaAI_ground_zero/frontend/src/components/drafting/DraftingWorkspace.jsx` | Init form: case/client selector from `users/filter_with_details/`, personal-draft toggle, state/district/court cascade from `users/get-states|districts|courts`, language selector from `aidrafts/get_supported_languages`. `handleCreateDraft` sends `draft_for`, `location`, `language`. Saved-drafts sidebar calls `aidrafts/get_user_saved_drafts_v2` (no trailing slash). Section titles normalised: `section_name \|\| section_title \|\| title`. |
| `DraftingWorkspace.jsx` — restored legacy drafting workflow | `mamlaAI_ground_zero/frontend/src/components/drafting/DraftingWorkspace.jsx` | Fixed slash-sensitive `aidrafts/get_draft_sections` calls that caused 404s on route load. Restored text-start and upload-start draft creation, paged/searchable Load Draft, Load Template, multi-entry `draft_for` selection, draft save/rename, draft-for metadata fetch, AI suggestion count, persisted section-title edits, section add/delete/reorder/history, revert-to-original, post-creation location updates via `aidrafts/set_location`, working drag-and-drop section reordering, legacy-style confirm-before-leaving back to draft selection, timestamp-based default draft names, hidden AI panel on the init screen, multipart template/source uploads aligned with the old frontend, and stable saved-draft `draft_id` reuse so repeated saves update the same saved draft entry instead of creating duplicates. |
| `CalendarPage.jsx` — advanced legal calendar in the new UI | `mamlaAI_ground_zero/frontend/src/components/calendar/CalendarPage.jsx` | The new MamlaAI app now has the actual calendar screen wired at `/calendar`: a Stitch-inspired legal calendar shell around FullCalendar with left mini-calendar + event-type filters, center month/week/day plus agenda view, right-side agenda/deadline rail, create/edit modal, conflict check flow, drag/drop updates, and case/client-aware intake sourced from `users/filter_with_details/`. The page talks to `calendar/events/`, `calendar/events/<id>/`, and `calendar/conflicts/check/` in the new UI codepath instead of the legacy `frontend_webpack` app. Conflict checks are now advisory-only in the UI: the dialog shows the overlap reasons and suggested next slot, but the user reschedules manually instead of the frontend auto-calling conflict resolution strategies. A follow-up fix also normalizes recurring instances that arrive with zero-duration `start/end` pairs so FullCalendar renders them reliably, and series-style create/update/delete now refetch the current date range instead of locally appending just the single event returned by the write API. |
| Calendar UX simplification for linked multi-day events | `mamlaAI_ground_zero/frontend/src/components/calendar/CalendarPage.jsx` | The create modal no longer asks the user to decide recurrence semantics up front for multi-day events. If the end date is later than the start date, the UI now treats the event as a linked daily series automatically and explains that behavior inline. Series scope (`only once`, `this and following`, `entire series`) is shown only when editing an existing linked chain. Time inputs now snap to 15-minute increments to match a more calendar-like workflow. |
| Calendar backend hardening for recurring chains and notifications | `Legalv1/calendar_management/views.py` · `Legalv1/calendar_management/routes/createupdateevents.py` · `Legalv1/calendar_management/tasks.py` · `Legalv1/core/email_templates.py` · `Legalv1/utilities/routes/utils.py` | Closed several gaps between the strong legacy service layer and the new REST/MamlaAI calendar usage. `POST /api/calendar/events/` now sends creation notifications just like the legacy `add-event/` path. `PUT /api/calendar/events/<id>/` now infers changed fields when the frontend omits `updatedFields`, routes updates through the recurring-aware service layer, and keeps participant copies plus notification emails in sync for both single events and series edits. `DELETE /api/calendar/events/<id>/` now infers recurring metadata from the stored event instead of relying on the caller to supply it. The service layer now implements real `this and following` behavior by splitting active series keys at the selected occurrence, preserving earlier links while updating or deleting the selected-and-future segment. Calendar emails are now delivered separately to the creator and each participant instead of exposing participant addresses through shared CC headers, and the shared email utility now strips empty recipients safely. A related async bug for participant-side one-off series cleanup was also fixed by matching party-B records by `email` instead of `user_id`. |
| Calendar create persistence fix for multi-day events | `Legalv1/calendar_management/routes/createupdateevents.py` | Fixed a MongoDB write conflict in `create_new_event` where `last_updated_on` was being written in both `$set` and `$setOnInsert`, causing fresh creates to fail silently and leaving multi-day/series events missing from the UI. Owner-record upserts now seed `created_at` instead, while `last_updated_on` continues to be updated through the normal write path. |
| Canonical `www` host redirect for production site | `mamla.ai` · `nginx_mamla.ai_optimized.conf` | Added explicit `www.mamla.ai` redirect server blocks on both HTTP and HTTPS so `www` always canonicalizes to `https://mamla.ai/...` instead of depending on the shared site block selection. This avoids host-specific 404s when the live server matches the wrong virtual host for `www`. |
| Production start script now builds the active frontend | `start.sh` · `README.md` | Fixed deployment mismatch where production builds were still targeting `frontend_webpack` while Nginx serves `mamlaAI_ground_zero/frontend/dist`. This left the live site without the build artifacts the active site config expects and could surface as Nginx 404s on `/`. |
| Calendar list reads now merge duplicate owner documents | `Legalv1/calendar_management/routes/createupdateevents.py` | Fixed a backend read-path bug where `get_all_events_for_user()` assumed a single `user_details` document per `user_id` and returned only the first Mongo match. If duplicate owner rows existed, create could save a new series into one row while list returned meetings from another, making freshly created events appear to vanish from the UI. The list path now merges meetings across all matching owner rows and prefers the most recently updated copy for duplicate meeting ids. |
| Calendar REST status normalization fix | `Legalv1/calendar_management/views.py` · `Legalv1/calendar_management/routes/createupdateevents.py` | Fixed the live create/list mismatch where REST-created events were saved without the legacy `Status` field expected by the list filter. New REST writes now persist `Status`/`status` as active by default, and list reads treat missing status as active so already-saved meetings created during the bug window become visible again without manual DB repair. |
| Calendar series delete/update compatibility fix | `Legalv1/calendar_management/routes/createupdateevents.py` · `mamlaAI_ground_zero/frontend/src/components/calendar/CalendarPage.jsx` | Fixed recurring series operations for events created during the earlier status-field bug window. The recurring service layer now treats missing `Status` the same way the list path does, so `this and following` and `entire series` operations no longer fail with `Meeting Not Found`. The MamlaAI calendar also now deletes through the REST `DELETE /api/calendar/events/<id>/` endpoint instead of the legacy `delete-event/` route, avoiding the misleading 401 response shape from the old path. |
| Calendar conflict dialog render hardening | `mamlaAI_ground_zero/frontend/src/components/calendar/CalendarPage.jsx` | Fixed a production React crash when checking conflicts on existing events. The backend returns `recommendations.next_available_slot` as an object `{start, end, label}`, and the UI was rendering that object directly in JSX. The calendar now formats conflict summaries, reasons, and suggested slots into strings before rendering, so conflict checks no longer trip the error boundary. |
| Calendar conflict intelligence now reflects real overlaps | `mamlaAI_ground_zero/frontend/src/components/calendar/CalendarPage.jsx` | The sidebar conflict card no longer depends only on stored `conflict_status` values, which were often left as `clear`. The new UI now derives conflict counts from actual time overlaps in the currently loaded events, so overlapping matters show up in the “Conflict Intelligence” summary even when older records were never explicitly flagged. The conflict dialog also now lets users save anyway after acknowledging a clash, persisting the event with a conflict marker instead of blocking creation outright. |
| MamlaAI eCourts litigant search now uses live backend defaults and cache | `mamlaAI_ground_zero/frontend/src/components/ecourts/LitigantSearch.jsx` · `mamlaAI_ground_zero/frontend/src/components/ecourts/common/ecourtsApi.js` · `mamlaAI_ground_zero/frontend/src/components/ecourts/common/useSearchCache.js` | Replaced the placeholder litigant search endpoint with the real `/api/ecourts/search/` flow using `search_type: 'litigant'`, restores `GET /api/ecourts/defaults/litigants/` on blank entry, and reuses the shared 30-minute sessionStorage cache so the new UI behaves like the proven older frontend instead of opening on an empty state. |
| MamlaAI no longer logs users out on eCourts partner-token failures | `mamlaAI_ground_zero/frontend/src/services/api.js` | The active frontend used to redirect to `/login` on every HTTP 401, including backend-proxied partner API failures like `{ code: 'INVALID_TOKEN' }` from the eCourts service. The interceptor now treats those as integration errors instead of user-session failures, so eCourts auth issues surface in-page without logging the user out. |
| eCourts default landing caches now survive the real refresh window | `Legalv1/ecourts_api/tasks.py` · `Legalv1/ecourts_scraper/cache/cache_manager.py` | Fixed a backend expiry bug that could blank the eCourts landing pages even when the feature was otherwise healthy. Default datasets were cached under an unregistered data type, so they silently fell back to a 24-hour TTL; that made weekly lawyer defaults expire almost immediately and made daily defaults disappear whenever Beat missed a run. The default-population tasks now set explicit daily/weekly TTLs and refuse to overwrite an existing cache entry with an empty result set. |
| Cause List Browser regained eCourts home navigation in MamlaAI | `mamlaAI_ground_zero/frontend/src/components/ecourts/CauseListBrowser.jsx` | Added the same top-level back-to-home control used by the other active eCourts screens so users can return to the eCourts landing page without relying on browser navigation. |
| MamlaAI Case Detail now matches the live eCourts direct-API shape | `mamlaAI_ground_zero/frontend/src/components/ecourts/CaseDetail.jsx` · `mamlaAI_ground_zero/frontend/src/components/ecourts/common/ecourtsApi.js` | Replaced the placeholder case-detail screen with the shared helper layer (`getCaseByCnr`, `getCaseOrders`, `refreshCase`, `downloadOrder`) and the normalized backend payload from `ecourts_api`. The page now supports refresh, authenticated PDF downloads, party/advocate drill-through into the existing litigant and lawyer search routes, tagged-matter navigation, and richer timeline/metadata sections without depending on old MUI-only routes. |
| MamlaAI eCourts home now surfaces live backend context instead of a static shell | `mamlaAI_ground_zero/frontend/src/components/ecourts/EcourtsHome.jsx` | The active landing page now uses SPA-safe quick CNR navigation, free court-structure stats from `GET /api/ecourts/court-structure/`, recent search recovery from the existing sessionStorage cache, and preview cards from `GET /api/ecourts/defaults/{cases,lawyers,litigants}/`. When defaults are unavailable, the page now degrades gracefully with actionable empty states instead of looking blank. |
| MamlaAI Cause List Browser now uses the real court hierarchy | `mamlaAI_ground_zero/frontend/src/components/ecourts/CauseListBrowser.jsx` · `mamlaAI_ground_zero/frontend/src/components/ecourts/common/ecourtsApi.js` | Replaced the old free-text placeholder form with the direct-API hierarchy flow: `states → districts → court complexes → court numbers`, plus available-date chips from `GET /api/ecourts/causelist/dates/`. Searches now send the backend’s actual parameter names (`state`, `districtCode`, `courtComplexCode`, `courtNo`, and `q`/`advocate`/`litigant`/`judge`) instead of guessed fields like `court_name`, so the active MamlaAI cause-list screen finally matches the live backend contract. |
| Calendar recurring regression coverage | `Legalv1/scripts/calendar_recurring_regression.py` | Added a dedicated regression script that creates a temporary linked series in Mongo, verifies REST-facing series metadata, validates `this and following` series splitting, and checks single-occurrence deletion cleanup before removing the temporary record. Current result: `calendar recurring regression: PASS`. |
| `CourtUpdates.jsx` — subscription management | `mamlaAI_ground_zero/frontend/src/components/courts/CourtUpdates.jsx` | `SubscriptionPanel` added: view/add/remove subscribed courts using `todaysupdates/get-subscriptions/`, `subscribe-court/`, `unsubscribe-court/`. State/district/court cascade for subscribe. Paralegal-variant endpoints supported. Pagination and filter tabs (All / Today / Critical). |
| `DocumentWorkspace.jsx` — Document Intel parity and usability pass | `mamlaAI_ground_zero/frontend/src/components/documents/DocumentWorkspace.jsx` · `mamlaAI_ground_zero/frontend/src/features/chatDocsSlice.js` · `Legalv1/talkdoc/views.py` · `Legalv1/talkdoc/tasks.py` · `Legalv1/talkdoc/urls.py` · `Legalv1/settings.py` · `start_backend.sh` · `start.sh` · `nginx_mamla.ai_optimized.conf` · `mamla.ai` · `Legalv1/scripts/backfill_talkdoc_document_metadata.py` | Fixed the stale session prepend bug and expanded the new TalkDoc workspace to behave like a production RAG tool instead of a static shell. Uploads now return normalized doc metadata, the UI polls document status until indexing completes, selected-doc cards reflect fresh backend status, and users can see queued/processing/indexing/indexed/failed states plus last update and failure details. General legal chat now works without any selected document, first-message session creation uses the newly created session id correctly, and the layout adds `Docs`, `Split`, and `Chat` focus modes with a draggable desktop divider so users can work chat-only or doc-only when needed. Backend ingest now records stage transitions (`queued → extracting → chunking → embedding → indexing → indexed/failed`) and DEBUG runs now surface INFO logs in Django/Celery instead of suppressing them. The follow-up pass added inline document preview/download, chat history with rename/delete, optional case/client context on uploads and new chats, timestamped default chat names, and user-safe error summaries so raw backend paths or tracebacks are not shown in the UI. The latest pass makes session scope authoritative: each chat now holds its own locked `doc_ids` and `matter`, retrieval uses only that stored session scope, and the rebuilt TalkDoc screen separates saved sessions, locked context, and the active conversation so users can exit the current session and start a fresh scoped chat without mutating prior context. The newest hardening pass restores visible rename/delete controls, switches PDF preview to authenticated blob loading instead of direct protected URLs, and adds API-backed case/client suggestions while still allowing freeform entries for new ids. The current cleanup pass replaces the overcrowded single-shell view with a true two-window TalkDoc flow: an initialization library for upload/delete/load-chat tasks and a separate document-viewer plus chat work window in the live theme. It also adds document deletion through `DELETE /api/talkdoc/documents/<doc_id>/`, so users can remove uploaded files directly from the setup window while chat deletion remains available from both setup and active work views. The latest expansion adds image uploads with multimodal OCR fallback, preserves table content from PDF/DOCX/CSV/XLSX files during indexing, rasterizes scanned PDF pages through the same multimodal OCR path when no text layer is present, renders images inline in the work window, lets users upload new files into an already-running chat so they are attached to the active session without resetting the conversation, and now uses the backend case-client mapping to auto-fill or constrain the client field when a case is selected in TalkDoc setup. The current fix pass further makes client autofill resilient when the backend only returns phone/email/name instead of a formal `client_id`, replaces the fragile free-text case/client setup inputs with the same drafting-style case/client grid selector used in DraftingWorkspace, keeps blank-client cases visible in that selector, exposes case/client document filters in the setup library, stores timestamped document display names for clearer chat citations, denormalizes case/client metadata onto `rag_documents`, aligns the tracked live Nginx site file used for `/etc/nginx/sites-available/mamla.ai`, and ships a one-off backfill script that was run successfully against the existing TalkDoc documents. |
| `CaseDetail.jsx` — null date guard | `mamlaAI_ground_zero/frontend/src/components/cases/CaseDetail.jsx` | `HearingRow` now checks `h.date` and `h.next_date` before constructing `Date` objects to prevent crash on hearings without dates. |
| MamlaAI client onboarding now matches the live users API contract | `mamlaAI_ground_zero/frontend/src/components/clients/ClientOnboarding.jsx` | Fixed the rebuilt client screen to use the field names and response shapes actually returned by the backend (`fname`/`lname`/`phone_number`/`client_id` instead of assumed `firstname`/`lastname`/`phone`). Previously onboarded clients now render again, update and invite flows refetch correctly instead of assuming the API returns full client objects, and the old “link existing user” workflow has been restored in the new UI via `check-existing-user/` and `onboard-existing-client/`. |
| Flat client list now uses the same mapping data as Drafting and Document Intel | `Legalv1/users/routes/usermetadata.py` · `Legalv1/users/supabase_views.py` | `GET /api/users/filter_with_details/` was already surfacing lawyer→client mappings correctly, which is why the Drafting and Document Intel grids stayed populated. The newer `GET /api/users/clients/` wrapper was flattening that same helper incorrectly: it expected lowercase `fname`/`lname` fields and embedded client ids that the helper did not include, so mapped clients disappeared from the flat list even though they still existed in Mongo and Supabase metadata. The helper and wrapper now both include `client_id` / `user_id` plus lowercase aliases, so the new client screen and the older grid-based flows are aligned to the same underlying data. |
| Mamla Brain framework foundation | `Legalv1/mamla_brain/` · `Legalv1/Legalv1/settings.py` · `Legalv1/Legalv1/urls.py` · docs | Added the first working Mamla Brain backend foundation as a self-standing domain reasoning framework. The new Django app exposes `/api/brain/v1/` health, document upload/listing, session CRUD, document-Q&A, Case Companion, and admin API-key endpoints. It implements dual auth (Supabase or `X-Brain-API-Key`), tiered LLM routing (`t1`/`t2`/`t3`), reusable domain profiles for `legal`, `banking`, and `markets`, knowledge-base retrieval helpers, and ingestion commands for domain source corpora. The implementation reuses TalkDoc document storage/vector search rather than duplicating ingestion. |
| Mamla Brain legal KB starter corpus | `Legalv1/mamla_brain/legal_kb_sources/` · `docs/07-mamla-brain-framework.md` | Added five original, section-structured starter legal knowledge-base files covering civil procedure, criminal procedure, evidence, contract disputes, and limitation/interim relief. These seed documents are intended for ingestion and retrieval validation without copying external statute text. The framework doc now reflects that KB seeding has started, while full primary-law and case-law corpus work remains pending. |
| Mamla Brain statute-oriented legal maps | `Legalv1/mamla_brain/legal_kb_sources/` · `docs/07-mamla-brain-framework.md` | Expanded the legal KB with six additional original source packs organized around CPC, CrPC, Evidence, IPC, NI Act cheque dishonour, and Specific Relief remedy selection. These files are written as practice-oriented section maps referencing statutory themes and section families without reproducing verbatim legislative text, giving Mamla Brain stronger legal retrieval coverage while staying within an original-summary corpus model. |
| Mamla Brain precedent-oriented legal notes | `Legalv1/mamla_brain/legal_kb_sources/` · `docs/07-mamla-brain-framework.md` | Added five original case-note packs organized around bail and personal liberty, civil-vs-criminal overlap, electronic evidence, injunction/specific relief, and NI Act presumption doctrine. These files summarize recurring Indian precedent patterns in original language so Mamla Brain can retrieve precedent-style reasoning without storing copied judgment text or headnotes. |
| Mamla Brain dispute-family precedent notes | `Legalv1/mamla_brain/legal_kb_sources/` · `docs/07-mamla-brain-framework.md` | Extended the precedent corpus with three more original packs focused on property/title and possession disputes, matrimonial/custody/maintenance matters, and company/director/vicarious liability analysis. This gives Mamla Brain better retrieval coverage for several common litigation categories beyond generic doctrine and procedural themes. |
| MamlaAI quota-aware drafting and document UX | `mamlaAI_ground_zero/frontend/src/components/drafting/DraftingWorkspace.jsx` · `mamlaAI_ground_zero/frontend/src/components/documents/DocumentWorkspace.jsx` · `mamlaAI_ground_zero/frontend/src/features/entitlementsSlice.js` · `docs/00-agent-quickref.md` · `docs/03-frontend-webpack.md` | The active frontend now surfaces quota state where users actually trigger AI actions instead of only in the shell. Drafting shows inline AI-suggestion warnings, wallet-credit charge feedback, and disables the AI assistant when suggestion usage is blocked. Document Workspace now shows Mamla Brain quota notices inside the chat pane and disables chat input when account access is unavailable, so the workspaces no longer fail as generic errors or silently ignore entitlement state. |
| Live entitlement refresh + TalkDoc quota enforcement | `Legalv1/talkdoc/views.py` · `mamlaAI_ground_zero/frontend/src/features/entitlementsActions.js` · `mamlaAI_ground_zero/frontend/src/AppContent.js` · `mamlaAI_ground_zero/frontend/src/components/drafting/DraftingWorkspace.jsx` · `mamlaAI_ground_zero/frontend/src/components/documents/DocumentWorkspace.jsx` · `docs/02-backend-legalv1.md` · `docs/04-api-reference.md` | TalkDoc document chat now consumes the same `brain_doc_analysis` entitlement used by Mamla Brain and returns a shared `quota` payload on success or exhaustion. The active frontend now refetches `users/entitlements/summary/` after quota-consuming drafting and document actions so header and dashboard counters update immediately instead of waiting for a full auth refresh. |
| AI draft generation quotas + low-latency legal-topic guard | `Legalv1/ai_draft/views.py` · `Legalv1/talkdoc/views.py` · `mamlaAI_ground_zero/frontend/src/components/drafting/DraftingWorkspace.jsx` · `docs/02-backend-legalv1.md` · `docs/03-frontend-webpack.md` · `docs/04-api-reference.md` | Draft creation endpoints now consume `ai_draft_generation` and return `quota` payloads, so the active frontend can refresh entitlement state immediately after creating a draft from prompt, template, or case document. In TalkDoc general-chat mode, obviously non-legal prompts are filtered by a local keyword heuristic before any LLM call, reducing cost without adding another model hop or visible latency. |
| Split TalkDoc usage into document analysis vs general legal chat | `Legalv1/core/entitlements.py` · `Legalv1/talkdoc/views.py` · `mamlaAI_ground_zero/frontend/src/components/layout/TopBar.jsx` · `mamlaAI_ground_zero/frontend/src/components/dashboard/Dashboard.jsx` · `mamlaAI_ground_zero/frontend/src/components/documents/DocumentWorkspace.jsx` · `docs/00-agent-quickref.md` · `docs/02-backend-legalv1.md` · `docs/03-frontend-webpack.md` · `docs/04-api-reference.md` | Added a separate `general_legal_chat` entitlement bucket so no-document TalkDoc sessions can be more generous than document-backed analysis. Backend routing now chooses the bucket from `session.has_docs`, and the active frontend exposes both counters in the top bar, dashboard, and document workspace while using the correct lock/warning state for the active session mode. |
| Reduced TalkDoc per-message API fan-out | `mamlaAI_ground_zero/frontend/src/features/entitlementsSlice.js` · `mamlaAI_ground_zero/frontend/src/components/documents/DocumentWorkspace.jsx` · `docs/03-frontend-webpack.md` | TalkDoc chat sends no longer refetch `users/entitlements/summary/` and `talkdoc/sessions/` after every message. The query response already contains the updated feature quota, so the frontend now patches the entitlement slice locally and updates the active session timestamp in-place. This trims the normal per-message path from three API calls to one, improving responsiveness without changing quota semantics. |
| TalkDoc session-bundle charging | `Legalv1/talkdoc/views.py` · `mamlaAI_ground_zero/frontend/src/components/documents/DocumentWorkspace.jsx` · `docs/02-backend-legalv1.md` · `docs/03-frontend-webpack.md` · `docs/04-api-reference.md` | Changed TalkDoc charging from per-message to a bounded session window. One successful TalkDoc charge now opens up to 10 chats in the active session before the next quota unit is consumed. This keeps the product feeling session-based to users without making long-lived chats permanently free. The workspace now surfaces the bundle in quota notices so users can see when the next charge will happen. |
| Active frontend visual refresh + doc pointer cleanup | `mamlaAI_ground_zero/frontend/src/index.css` · `mamlaAI_ground_zero/frontend/tailwind.config.js` · `mamlaAI_ground_zero/frontend/src/components/landing/LandingPage.jsx` · `mamlaAI_ground_zero/frontend/src/components/layout/AppShell.jsx` · `mamlaAI_ground_zero/frontend/src/components/layout/Sidebar.jsx` · `mamlaAI_ground_zero/frontend/src/components/layout/TopBar.jsx` · `README.md` · `docs/README.md` · `docs/00-agent-quickref.md` · `docs/01-architecture-overview.md` · `docs/03-frontend-webpack.md` | Reskinned the active MamlaAI frontend from the earlier warm brown palette to a deep navy, white, and graphite system inspired by Supreme Court and chamber visuals. The refresh stays at the token and shared-shell layer so routes, API contracts, and existing workflows remain intact. A follow-up pass hardened readability by swapping in `Source Serif 4` + `IBM Plex Sans` and raising contrast on navigation, hero copy, pills, footer text, and shell controls where the earlier styling felt too faint. Documentation now points to `mamlaAI_ground_zero/frontend` as the active frontend and marks `frontend_webpack` as the previous UI only. |
| Public-entry alignment + drafting editor-first pass | `mamlaAI_ground_zero/frontend/src/components/auth/AuthShowcase.jsx` · `mamlaAI_ground_zero/frontend/src/components/auth/Login.jsx` · `mamlaAI_ground_zero/frontend/src/components/auth/Signup.jsx` · `mamlaAI_ground_zero/frontend/src/components/landing/LandingPage.jsx` · `mamlaAI_ground_zero/frontend/src/components/drafting/DraftingWorkspace.jsx` · `docs/03-frontend-webpack.md` | Unified login/signup with the landing-page navy theme, replaced the earlier marketing-heavy auth copy with straightforward product wording, tightened the auth layouts so the forms sit in view without feeling pushed down, and kept direct links back to the landing page. The landing page hero now rotates through bench, chamber, and draft-control scenes, uses a fully opaque top nav for first-paint readability, and adds an interactive original illustration gallery instead of external Google images. The drafting workspace now prioritizes document reading and editing by making outline and AI rails collapsible and by removing post-load location edits from the editor surface so the location chosen at draft creation stays final. |
| Shared blocking overlay + landing motion pass | `mamlaAI_ground_zero/frontend/src/features/uiSlice.js` · `mamlaAI_ground_zero/frontend/src/components/common/GlobalLoadingOverlay.jsx` · `mamlaAI_ground_zero/frontend/src/App.js` · `mamlaAI_ground_zero/frontend/src/store.js` · `mamlaAI_ground_zero/frontend/src/index.css` · `mamlaAI_ground_zero/frontend/src/components/auth/Login.jsx` · `mamlaAI_ground_zero/frontend/src/components/auth/Signup.jsx` · `mamlaAI_ground_zero/frontend/src/components/auth/ResetPassword.jsx` · `mamlaAI_ground_zero/frontend/src/components/drafting/DraftingWorkspace.jsx` · `mamlaAI_ground_zero/frontend/src/components/documents/DocumentWorkspace.jsx` · `mamlaAI_ground_zero/frontend/src/components/calendar/CalendarPage.jsx` · `mamlaAI_ground_zero/frontend/src/components/landing/LandingPage.jsx` | Added a shared app-level loading overlay so long-running auth, drafting, TalkDoc, and calendar actions block stray clicks without changing request timing. The overlay is driven by a small `uiSlice` counter and shown from the app root. TalkDoc now blocks during session opening, uploads, session creation, document removal, chat deletion, and query sends; calendar now blocks during initial event load, conflict checks, saves, deletes, and drag-move updates. The landing page also gained lightweight page-load and scene-transition motion using CSS-only utilities so the public experience feels more polished without adding a runtime animation dependency. |
| Top bar controls now do real work | `mamlaAI_ground_zero/frontend/src/components/layout/TopBar.jsx` · `mamlaAI_ground_zero/frontend/src/components/documents/DocumentWorkspace.jsx` · `docs/03-frontend-webpack.md` | Replaced the dead search, notification, and help controls in the protected app shell. Search now works as quick navigation across the live product areas, saved draft/session matches, case-ID shortcuts that open Document Intel with active matter filters, and direct CNR case-detail shortcuts into eCourts. Notifications show actionable alerts from quota state plus `dashboard/home/` upcoming events and recent updates, and the help control opens real support/workspace actions instead of a non-functional icon. |
| Scraper-first eCourts runtime activated | `Legalv1/Legalv1/urls.py` · `Legalv1/ecourts_scraper/*` · `Legalv1/settings.py` · docs | `/api/ecourts/` now points to `ecourts_scraper` instead of the retired partner-token `ecourts_api` path. Added Mongo-backed `ecourts_reference_data` for stitched terminal dropdown metadata, new reference endpoints for district-court hierarchy and static module configuration, Capsolver support in the scraper CAPTCHA pipeline, and a new Beat task to materialize reference datasets. `ecourts_api` remains in the repo only as deprecated reference code and is no longer installed or scheduled at runtime. |
| Scraper-first eCourts terminal routes expanded | `mamlaAI_ground_zero/frontend/src/components/ecourt_scrapper/*` · `mamlaAI_ground_zero/frontend/src/AppContent.js` · `Legalv1/ecourts_scraper/views.py` · docs | Added real scraper-first route surfaces for `/ecourts/case-status`, `/ecourts/court-orders`, `/ecourts/cause-list`, and `/ecourts/caveat`, plus a shared frontend polling helper for queued scraper jobs. Case status now uses a stitched terminal layout with live advocate search and explicit staged tabs for unsupported modes, court orders resolve through cached CNR case data, cause list uses the high-court scraper flow, and caveat is shown as staged instead of silently falling back to old partner assumptions. Backend `POST /api/ecourts/search/` now rejects unsupported `search_type` values with `400` so unfinished modes fail honestly rather than being misrouted. |
| High Court party-name search reactivated in scraper flow | `Legalv1/ecourts_scraper/views.py` · `Legalv1/ecourts_scraper/tasks.py` · `Legalv1/ecourts_scraper/scrapers/highcourt.py` · `mamlaAI_ground_zero/frontend/src/components/ecourt_scrapper/CaseStatusTerminal.jsx` · docs | Reintroduced a real scraper-backed party-name case-status search for High Court using verified live selectors (`CSpartyName`, `petres_name`, `rgyearP`, `radP/radD/radB`). The terminal now lights up the stitched party-name tab only on High Court, requires registration year, and keeps District Court party-name mode explicitly staged until its selectors are verified. |
| Backend startup now fails fast on empty env config | `start.sh` · `start_backend.sh` · `Legalv1/Legalv1/settings.py` · `Legalv1/core/apps.py` · `Legalv1/core/init_clients.py` · `Legalv1/users/routes/encryption.py` · docs | Replaced the previous noisy background failures with explicit configuration validation. Startup now refuses to launch when `Legalv1/legalenv` has empty Supabase, encryption, or Mongo settings, and Django raises clearer `ImproperlyConfigured` messages instead of surfacing downstream Supabase/PyMongo library errors. |
| Env-name compatibility aligned with checked-in legalenv | `Legalv1/ecourts_scraper/constants.py` · `Legalv1/talkdoc/search.py` · docs | Added compatibility aliases so the current env file works without renaming secrets by hand: the scraper now accepts `CAPSOLVER_API` in addition to the earlier Capsolver key names, and TalkDoc OpenSearch helpers now fall back from `RAG_OS_*` to `OPENSEARCH_*` while correctly omitting auth when username/password are blank. |
| Dev restart flow no longer trips on stale port 3000 | `start.sh` · docs | Fixed the development start script to stop existing services before checking whether port 3000 is free. Previously a leftover frontend process caused an immediate false-negative restart failure even though the script already had cleanup steps later in the flow. |
| eCourts terminal runtime regressions corrected | `mamlaAI_ground_zero/frontend/src/components/ecourt_scrapper/CaseStatusTerminal.jsx` · `mamlaAI_ground_zero/frontend/src/components/ecourt_scrapper/EcourtsTerminal.jsx` · `mamlaAI_ground_zero/frontend/src/components/ecourt_scrapper/CauseListTerminal.jsx` · `Legalv1/ecourts_scraper/scrapers/causelist.py` · `Legalv1/ecourts_scraper/views.py` · docs | Removed the stale `LIVE_MODE_KEYS` reference that was crashing the case-status screen, made the CNR lookup card on the scraper terminal directly focus the quick-lookup input, and repaired the High Court cause-list scraper against the live site. The scraper now enters cause list through the main HC menu, solves the visible captcha, fills the live `causelist_date` field, and only advertises the verified daily cause-list mode while non-daily stitched variants return `400` instead of failing later in the scrape. |
| CAPTCHA fallback restored and Capsolver diagnostics expanded | `Legalv1/ecourts_scraper/infra/captcha.py` · `Legalv1/requirements.txt` · `Legalv1/environment.yml` · docs | Added `easyocr` back to the backend dependency manifests and installed it into `myenv` so the local OCR fallback works again when Capsolver does not return a usable result. The Capsolver branch now logs accepted `task_id` values plus poll status/error fields, making it easier to distinguish task-lifecycle failures such as `ERROR_TASK_NOT_FOUND` from plain connectivity or key problems. |
| Capsolver ImageToText flow corrected | `Legalv1/ecourts_scraper/infra/captcha.py` · docs | Fixed the Capsolver integration to match the current `ImageToTextTask` contract. The OCR result is returned directly from `createTask`; the previous code incorrectly called `getTaskResult`, which caused every accepted task to fail with `ERROR_TASK_NOT_FOUND`. The scraper now consumes the direct `createTask` response instead of treating ImageToText as an asynchronous polling workflow. |
| High Court submit-selector collision and background cause-list noise fixed | `Legalv1/ecourts_scraper/scrapers/highcourt.py` · `Legalv1/ecourts_scraper/tasks.py` · docs | High Court case-status by party name was sharing the same page as cause list, but the scraper still used the generic `.Gobtn` submit selector, which matched both the case-status and cause-list Go buttons. The scraper now picks the correct submit control per method (`CNR`, `party`, `advocate`). Separately, the beat-driven cause-list refresh now skips legacy `subscribed_courts` entries that do not include scraper ids, preventing unrelated hidden-captcha warnings from appearing while a user is running case-status searches. |

---

## 3. What Was Intentionally Not Changed

- **WhatsApp module:** Left as-is; to be revisited later.
- **Existing API response shapes:** No mass change to error format so frontend contracts stay valid.
- **Commented `models.py` files:** Left in place; apps use raw MongoDB. Can be removed or replaced with ODM in a later pass.
- **Unused folder:** Not deleted; only documented. Safe to delete later if desired.
- **Full test coverage / TypeScript / API docs:** Not in scope; listed below as future improvements.

---

## 4. Incremental Improvement Plans

Use these as a **backlog**. Each item can be done independently.

### High impact, low risk

- [ ] **Backend:** Add `.env.example` (or document in README) listing all required env vars (`MONGO_URI`, `FRONTEND_URL`, `SUPABASE_*`, etc.).
- [ ] **Backend:** Add Brain admin env documentation or `.env.example` coverage for `BRAIN_ADMIN_EMAILS` if admin email allow-listing will be used in production.
- [ ] **Backend:** Fix duplicate imports in `Legalv1/Legalv1/urls.py` (repeated `from django.contrib import admin` and `path, include`).
- [ ] **Frontend:** Ensure `setupResponseInterceptors(navigate)` is called with the correct `navigate` (e.g. from a top-level component that has `useNavigate`), so 401/403 redirects work everywhere.
- [ ] **Both:** Add a short root README that points to `docs/README.md` and describes how to run backend and frontend (and env).

### Security and robustness

- [ ] **Backend:** Add rate limiting to password reset and email verification endpoints if not already present.
- [ ] **Backend:** Enforce `SECURE_SSL_REDIRECT = True` in production (e.g. via env).
- [ ] **Backend:** Validate required env vars at startup and fail fast with a clear message.
- [ ] **Frontend:** Replace weak token obfuscation in `securityUtils.js` with a clearer approach (e.g. rely on HttpOnly cookies where possible and document the rest).

### Consistency and maintainability

- [ ] **Backend:** Gradually adopt `core.response_utils.error_response` for new endpoints and, where safe, for refactors that don’t change existing client behavior.
- [ ] **Backend:** Extract constants for magic numbers (e.g. max sessions, OTP limits) and document them.
- [ ] **Backend:** Add indexes for frequently queried fields (e.g. `sessions.access_token`, `service_orders.order_id`) if not already present; consider running `ensure_indexes()` on deploy.
- [ ] **Frontend:** Replace remaining `alert()` or ad-hoc error UI with Snackbar/Alert where it makes sense.
- [ ] **Frontend:** Add loading/error states for critical flows (e.g. onboarding, draft save) where missing.

### Larger or later

- [ ] **Backend:** Break down the WhatsApp webhook handler into smaller functions and add tests when touching that feature.
- [ ] **Backend:** Add OpenAPI/Swagger (or similar) and keep it in sync with `docs/04-api-reference.md`.
- [ ] **Backend:** Add unit/integration tests for auth and critical user/draft flows.
- [ ] **Frontend:** Consider TypeScript for new or refactored modules.
- [ ] **Frontend:** Remove or archive `src/components/unused/` if no longer needed for reference.
- [ ] **Both:** Document deployment (e.g. Docker, env injection, health check usage).

---

## 5. How to Use This Doc for Continuation

- **“Continue the refactor”** → Pick the next unchecked item in §4 that matches the current goal (e.g. env example, rate limiting, or error response standardization).
- **“Fix a bug in auth”** → Use **02-backend-legalv1.md** (auth flow) and **04-api-reference.md** (users endpoints); ensure both frontend and backend use Supabase-only flows.
- **“Add a new API”** → Follow **04-api-reference.md** for path and auth convention; implement in the correct app (see **02-backend-legalv1.md**); call it via `AxiosInstance` on the frontend (see **03-frontend-webpack.md**).
- **“Onboard a new dev or AI”** → Start with **docs/README.md** and **01-architecture-overview.md**, then **02** and **03** for the layer being worked on, and **04** for endpoint details. Use **05** for context on what changed and what to do next.

When making incremental changes, update this document (and, if needed, **04-api-reference.md**) so the next reader or AI has an up-to-date picture.
